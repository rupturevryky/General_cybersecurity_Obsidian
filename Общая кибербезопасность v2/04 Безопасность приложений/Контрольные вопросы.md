# Контрольные вопросы по безопасности приложений

## Web-платформа

1. Из каких компонентов складывается origin?
2. Почему CORS не является серверной авторизацией?
3. В каких случаях браузер выполняет preflight-запрос?
4. Какие свойства дают cookies атрибуты `Secure`, `HttpOnly` и `SameSite`?
5. Почему TLS не защищает от XSS или ошибок контроля доступа?

## Аутентификация и сессии

1. Чем аутентификация отличается от авторизации?
2. Какие свойства должны быть у безопасного восстановления доступа?
3. При каких условиях возможен CSRF?
4. Какие проверки обязательны при приёме подписанного JWT?
5. Чем access token отличается от OpenID Connect ID token?

## Ввод и обработка данных

1. Почему универсальная «фильтрация опасных символов» не является надёжной защитой от XSS?
2. Чем server-side request forgery отличается от перенаправления браузера?
3. Какие проверки нужны для безопасной загрузки файлов?
4. Почему параметризованный SQL-запрос безопаснее конкатенации строк?
5. Как различие парсинга frontend и backend приводит к request smuggling?
6. Почему race condition нельзя надёжно исправить только ограничением скорости?

## API и Mobile

1. Что проверяется при object-level authorization?
2. Какие риски создаёт mass assignment?
3. Почему отключение GraphQL introspection не устраняет ошибки авторизации?
4. Какие сведения Android-приложение может раскрыть через manifest и логи?
5. Чем статический анализ APK дополняет динамический анализ?
